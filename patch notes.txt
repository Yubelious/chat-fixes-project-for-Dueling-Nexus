CSS FIXES PROJECT
**Patch Notes**
v1.3 Made minor adjustments to #socket-connection, added a black navbar, adjusted some elements to account for the new navbar, got rid of a lot of unneccesary scrollbars, article resizing now only affects the login register and recover pages, made numerous changes to the login register recover and profile pages, fixed all the hover animations I could find, News now hides when not in use, swapped the editor buttons to the left side the way I like them, added a background colour and line breaks to the siding tutorial, adjusted checkboxes and their labels, made major size changes to the editor and related elements, added the donate page, and added an entire section of code dedicated for resolutions under 1250px wide.
